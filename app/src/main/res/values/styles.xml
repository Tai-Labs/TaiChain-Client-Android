<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="color_bg_main">@color/white</item>
        <item name="text_one_style">@style/Text_one_light_style</item>
        <item name="text_two_style">@style/Text_two_light_style</item>
        <item name="item_bg">@style/item_light_bg</item>
        <item name="line_style">@style/line_light_style</item>
        <item name="empty_style">@style/empty_light_style</item>
        <item name="item_color__style">@color/bg_color</item>
        <!-- 没有输入线颜色-->
        <item name="colorControlNormal">@color/gray</item>
        <!-- 输入文字线颜色-->
        <item name="colorControlActivated">@color/zt_lu</item>
        <!-- 输入文字颜色-->
        <item name="colorControlHighlight">@color/gray</item>
    </style>

    <style name="ThemeLight" parent="AppTheme"></style>

    <style name="Text_one_light_style">
        <item name="android:textColor">@color/title_color</item>
    </style>

    <style name="Text_two_light_style">
        <item name="android:textColor">#999999</item>
    </style>

    <style name="item_light_bg">
        <item name="android:background">@color/bg_color</item>
    </style>

    <style name="line_light_style">
        <item name="android:background">@color/line</item>
    </style>

    <style name="empty_light_style">
        <item name="android:background">@mipmap/empty_data</item>
    </style>


    <style name="Theme.ToolBar.Menu" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- 设置-弹窗的背景-颜色-->
        <item name="android:colorBackground">@color/common_bg</item>
        <!-- 字体的颜色-->
        <item name="android:textColor">@color/common_h1</item>
        <!-- 字体大小-->
        <item name="android:textSize">16sp</item>
        <!-- 用于替换菜单的三个小白点，换成自已的图片-->
        <item name="actionOverflowButtonStyle">@style/ActionButton.Overflow.Menu</item>
        <!--用于控制menu弹出的位置，位于toolbar的下面，而不是上面-->
        <item name="actionOverflowMenuStyle">@style/OverflowMenuStyle</item>
        <!-- 用于修改item默认的分割线-->
        <item name="android:dropDownListViewStyle">@style/dropDownStyle</item>
        <item name="dropDownListViewStyle">@style/dropDownStyle</item>
    </style>

    <style name="ActionButton.Overflow.Menu" parent="android:style/Widget.Holo.Light.ActionButton.Overflow">
        <item name="android:src">@mipmap/ic_moreoverflow_mtrl_alpha</item>
    </style>
    <!--把该属性改为false即可使menu位置位于toolbar之下-->
    <style name="OverflowMenuStyle" parent="Widget.AppCompat.Light.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>
    </style>
    <!--用于修改popmenu的分割线-->
    <style name="dropDownStyle" parent="android:style/Widget.Holo.ListView.DropDown">
        <item name="android:listSelector">@drawable/touch_bg</item>
        <item name="android:divider">@color/common_divider_narrow</item>
        <item name="android:dividerHeight">0.5dp</item>
    </style>

    <!-- 自定义loading dialog -->
    <style name="loading_dialog" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">false</item><!--半透明-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item><!--模糊-->
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="MyDialog" parent="@style/AppTheme">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/btm</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="ToastDialog" parent="@style/android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="SplashTheme" parent="AppTheme">

        <item name="android:windowBackground">@mipmap/welcom_bg1</item>

        <item name="android:windowFullscreen">true</item>

    </style>
</resources>
